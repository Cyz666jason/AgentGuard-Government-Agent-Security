apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: agentguard
resources:
  - configmap.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - networkpolicy.yaml
  - pdb.yaml
  - hpa.yaml
  # Apply secretref.yaml only in clusters with the approved External Secrets
  # Operator and a reviewed ClusterSecretStore.
